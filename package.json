{
  "name": "loop-animation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Codex skill for turning concepts into interactive Three.js explainers and exporting them as HTML, MP4, GIF, or PNG.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "export:mp4": "npm run build && node scripts/export.mjs --format mp4",
    "export:gif": "npm run build && node scripts/export.mjs --format gif",
    "export:png": "npm run build && node scripts/export.mjs --format png"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.185.1",
    "puppeteer": "25.3.0",
    "typescript": "7.0.2",
    "vite": "8.1.5"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
